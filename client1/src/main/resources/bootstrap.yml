server:
  port: 8082
spring:
  profiles:
    active: prod
  application:
    name: client1
  cloud:
    config:
      #uri: http://localhost:8081/
      discovery:
        enabled: true
        service-id: config-server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka

