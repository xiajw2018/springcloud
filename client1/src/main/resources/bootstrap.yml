server:
  port: 8082
spring:
  profiles:
    active: prod
  application:
    name: client1
  cloud:
    config:
      uri: http://localhost:8081/
    stream:
      kafka:
        binder:
          brokers: 192.168.92.132:9092
# 其余配置都在github
management:
  endpoints:
    web:
      exposure:
        include: refresh,health


